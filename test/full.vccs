// Basic constant calls
P = a(x:[0,10]).Q(x);
Q(x:[0,1], y:[0,5]) = Q(x);
B = Q(1);

//// Declaration with interval and enum types
//Main(x:[0,10], y:[0,5]) = 'out(x).if x == 0 then A + B | 'write(x + 1).tau.Main(x + 1, y) \ {read, write} [read(x:[1,1]) : in(x:[1,1])];
//
//// Input with variable
InputProc = channel(msg:[0,20]).P;
//
//// Output with expression
OutputProc = channel(x:[10,20]).'channel(x + 1).Q(x);
//
//// Silent prefix
SilentProc = tau.P;
//
//// Conditional branching
CondProc(x:[0,10]) = if x != 0 then P;
//
//// Parallel composition
ParProc = (a(x:[10,20]).A) | B;
//
//// Sum composition
SumProc = A + B;
//
//// Deeply nested composition
DeepNest(x:[0,5]) = 'a(x).(if x > 0 then ( 'b(x + 1).(C(x + 1) + D) | 'c(x).tau.CondProc(x) ));
//
//// Renaming test
RenameTest = SilentProc [a(x:[0,10]) / b(x:[0,10])];
//
//// Restriction test
RestrictTest = ParProc \ {a:[10,20]};
//
//// ConstCall with complex args
//ComplexArgs(x:[0,5], y:[2,2], z:[1,2], a:[1,1], b:[1,2]) = F(3 * x + 1, y / 2, z + (a - b));
//
//// Combined expression with many constructs
//Main(x : [0,10]) = 'step(x).tau.Done;
//Mega = a(x:[0,10]).b(y:[0,10]).Main(x, y);